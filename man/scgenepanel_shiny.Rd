% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_shiny.R
\name{scgenepanel_shiny}
\alias{scgenepanel_shiny}
\title{Launch Interactive Gene Panel Shiny Application}
\usage{
scgenepanel_shiny(object = NULL)
}
\arguments{
\item{object}{A Seurat or SingleCellExperiment object. If NULL, loads example data}
}
\value{
Invisibly returns NULL. Function is called for its side effect of launching the Shiny application.
}
\description{
\code{scgenepanel_shiny} launches a Shiny web application for interactive
exploration of single-cell gene expression data with dynamic parameter
selection.
}
\examples{
# Example 1: Launch with example data
if (interactive()){
  scgenepanel_shiny()
}

# Example 2: Launch with seurat object
data <- readRDS(system.file("extdata", "human_panc_islets.Rds", package = "scGenePanel"))
if (interactive()){
  scgenepanel_shiny(data)
}

# Example 3: Launch with SCE object
data <- readRDS(system.file("extdata", "sce_object.Rds", package = "scGenePanel"))
if (interactive()){
  scgenepanel_shiny(my_data)
}

}
